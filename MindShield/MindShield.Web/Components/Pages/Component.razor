@page "/"
@using MindShield.Web
@using MindShield.Core
@using Microsoft.EntityFrameworkCore
@inject MindShieldDbContext DbContext
@inject ISafetyService SafetyService
@rendermode InteractiveServer

<div class="glass-panel">
    <h1>MindShield</h1>
    <p class="subtitle">
        Protecting: <strong>@(currentUser?.FullName ?? "Alex Rivera")</strong>
    </p>

    @* --- EXPANDED PLATFORM SELECTOR --- *@
    <div class="platform-selector" style="display: flex; gap: 8px; margin-bottom: 15px; align-items: center; flex-wrap: wrap;">
        <span style="color: rgba(255,255,255,0.7); font-size: 0.9rem; margin-right: 5px;">Target:</span>

        <button class="btn-pill @(selectedPlatform == "LinkedIn" ? "active-platform" : "")"
                @onclick='() => SetPlatform("LinkedIn")'>
            LinkedIn
        </button>

        <button class="btn-pill @(selectedPlatform == "Twitter" ? "active-platform" : "")"
                @onclick='() => SetPlatform("Twitter")'>
            X / Twitter
        </button>

        <button class="btn-pill @(selectedPlatform == "Instagram" ? "active-platform" : "")"
                @onclick='() => SetPlatform("Instagram")'>
            Instagram
        </button>

        <button class="btn-pill @(selectedPlatform == "TikTok" ? "active-platform" : "")"
                @onclick='() => SetPlatform("TikTok")'>
            TikTok
        </button>
    </div>

    <textarea @bind="postContent"
              @oninput="OnContentChanged"
              placeholder="What's on your mind?">
    </textarea>

    <div style="display: flex; justify-content: space-between; align-items: center; margin-top: 1rem;">
        <span style="color: rgba(255,255,255,0.5); font-size: 0.9rem;">@postContent.Length characters</span>

        <div style="gap: 10px; display: flex;">
            <button class="btn-primary" @onclick="AnalyzePost" disabled="@isScanning">
                @(isScanning ? "Scanning..." : "Check Safety")
            </button>

            @if (canPost)
            {
                <button class="btn-success" @onclick="SimulatePost">🚀 Post to @selectedPlatform</button>
            }
        </div>
    </div>

    @* --- PULSING ANALYZING STATE --- *@
    @if (isScanning)
    {
        <div class="analyzing-state">
            <div class="pulse-dot"></div>
            <span>AI is analyzing context for @selectedPlatform...</span>
        </div>
    }
    @* --- RESULT BOX WITH CONFIDENCE SCORE --- *@
    else if (safetyResult != null)
    {
        <div class="alert-box @alertClass">
            <div style="display: flex; justify-content: space-between; align-items: center;">
                <strong>@safetyResult.Status</strong>

                @* THE NEW CONFIDENCE SCORE BADGE *@
                @if (safetyResult.ConfidenceScore > 0)
                {
                    <span style="background: rgba(255,255,255,0.2); padding: 2px 8px; border-radius: 12px; font-size: 0.85rem;">
                        @safetyResult.ConfidenceScore% Confidence
                    </span>
                }
            </div>

            <p style="margin-top: 10px;"><b>Reason:</b> @safetyResult.Reason</p>

            @if (!string.IsNullOrWhiteSpace(safetyResult.Rewrite))
            {
                <p><b>Suggested Rewrite:</b> @safetyResult.Rewrite</p>
                <button class="btn-primary" style="margin-top: 10px;" @onclick="ApplyRewrite">
                    ✏ Use Suggested Rewrite
                </button>
            }

            <p style="margin-top: 10px;"><b>Recommended Action:</b> @safetyResult.Action</p>
        </div>
    }
</div>

@if (showGuardianPopup)
{
    <div class="guardian-toast">
        <span style="font-size: 1.5rem;">🔒</span>
        <div>
            <strong>Guardian Notified</strong><br />
            <small>Sent secure alert to Wife</small>
        </div>
    </div>
}

@code {
    private RealityProfile? currentUser;
    private string postContent = "";
    private SafetyResult? safetyResult;
    private string alertClass = "";
    private bool showGuardianPopup = false;
    private bool canPost = false;
    private bool isScanning = false;

    // Platform State
    private string selectedPlatform = "LinkedIn"; // Default

    protected override async Task OnInitializedAsync()
    {
        try
        {
            currentUser = await DbContext.RealityProfiles.FirstOrDefaultAsync();
        }
        catch
        {
            currentUser = new RealityProfile { FullName = "Alex Rivera" };
        }
    }

    private void SetPlatform(string platform)
    {
        selectedPlatform = platform;
        safetyResult = null;
        canPost = false;
        alertClass = "";
    }

    private async Task AnalyzePost()
    {
        if (currentUser == null || string.IsNullOrWhiteSpace(postContent)) return;

        isScanning = true;
        canPost = false;
        showGuardianPopup = false;
        safetyResult = null;
        StateHasChanged();

        // Pass the context string to the AI based on the platform
        string platformContext = selectedPlatform switch
        {
            "LinkedIn" => "Professional networking site. High recruiter visibility. Zero tolerance for aggressive rants, complaints about employers, or unprofessional behavior.",
            "Twitter" => "Fast-paced, casual microblogging. Opinions and sarcasm are common. Flag as Moderate if overly aggressive.",
            "Instagram" => "Highly visual, lifestyle-oriented network. Casual but curated. Focuses on personal branding.",
            "TikTok" => "Gen-Z focused, highly casual, trend-driven video platform. Very loose tone. Sarcasm and venting are common.",
            _ => "General public platform."
        };

        // Call the updated service
        safetyResult = await SafetyService.AnalyzeAsync(postContent, currentUser, selectedPlatform, platformContext);

        isScanning = false;

        if (safetyResult.Status == "SAFE")
        {
            alertClass = "alert-safe";
            canPost = true;
        }
        else
        {
            canPost = false;

            alertClass = (safetyResult.RiskLevel?.Equals("Severe", StringComparison.OrdinalIgnoreCase) == true)
             ? "alert-danger"
             : "alert-warning";

            if (safetyResult.Status == "DANGER" ||
                safetyResult.RiskLevel?.Equals("Severe", StringComparison.OrdinalIgnoreCase) == true)
            {
                showGuardianPopup = true;
            }
            else
            {
                showGuardianPopup = false;
            }
        }

        StateHasChanged();
    }

    private void SimulatePost()
    {
        if (safetyResult?.Status != "SAFE")
            return;

        alertClass = "alert-safe";

        safetyResult = new SafetyResult
        {
            Status = "SAFE",
            ConfidenceScore = 100,
            Reason = $"Successfully posted to {selectedPlatform}!",
            Rewrite = "",
            Action = "Post completed."
        };

        postContent = "";
        canPost = false;
    }

    private void ApplyRewrite()
    {
        if (safetyResult != null && !string.IsNullOrWhiteSpace(safetyResult.Rewrite))
        {
            postContent = safetyResult.Rewrite;
            safetyResult = null;
            canPost = false;
            alertClass = "";
        }
    }

    private void OnContentChanged(ChangeEventArgs e)
    {
        safetyResult = null;
        canPost = false;
        alertClass = "";
    }
}